package com.example.android_project.ui.theme

// DebtCalculator.kt

import com.example.android_project.data.GroupPerson

object DebtCalculator {
    fun calculateDebts(people: List<GroupPerson>): Map<String, Double> {
        // Implement the logic to calculate debts based on expenses
        // You may need to consider each person's contribution and calculate owed/owing amounts
        // Return a map where keys are person IDs and values are the amounts owed/owing
        return emptyMap()
    }
}
